{
  "name": "the-librarian-codex-plugin",
  "version": "0.4.1",
  "private": true,
  "description": "Codex plugin for The Librarian: memory + cross-harness handoffs over a remote Librarian MCP server.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build-bundle.mjs",
    "validate": "node scripts/validate.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check:release": "node --no-warnings scripts/check-release.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
